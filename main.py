print("Welcome to Band Name Generator!\n")
city = input("What's the name of the city you grew up in?\n")
print(city)
pet_name = input("What's your pet's name?\n")
print(pet_name)
print(f"Your band name could be {city} {pet_name}")

# this code is pretty straight forward
# the first variable (city) stores the first input from the user
# the second variable (pet_name) stores the second input from the user
# the final line of code adds both variables together to form a band name
